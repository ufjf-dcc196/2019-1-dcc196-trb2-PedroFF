<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.trabalho2">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".activity.PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.CadastrarTarefaActivity"/>
        <activity android:name=".activity.EditarTarefaActivity"/>
        <activity android:name=".activity.InserirTagActivity"/>
        <activity android:name=".activity.ListarTagActivity"/>
        <activity android:name=".activity.ListarTarefaActivity"/>
    </application>

</manifest>